#include "Keyable.h"

namespace plasma {
	Keyable::Keyable(Engine* engine, const std::wstring& name) : NamedObject(engine, name) {
		// Keyable has some list not known yet
	}
	
	Keyable::~Keyable() {
	
	}

};